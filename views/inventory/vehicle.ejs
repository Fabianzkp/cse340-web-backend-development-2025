<!-- The Content Heading -->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<!-- A Message Block -->
<%- messages() %>

<!-- The Inventory Items Display -->
<%- drill %>

<a href="/inv/maintenance/<%= vehicle.inv_id %>" class="link-back">View Maintenance Records</a>